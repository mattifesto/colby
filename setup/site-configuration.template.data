<?php

/**
 * Use this file to set constants and perform actions that are shared between
 * all instances of the site, for instance: development, test, and production.
 *
 * The colby-configuration.php file is different for each instance and is not
 * checked in. This file is the same for each instance and is checked in.
 *
 * No action is required in this file but common actions are setting constants
 * and loading libraries that are used by the site.
 *
 * Colby::loadLibrary('MyLibrary');
 *
 * The argument to Colby::loadLibrary() is a path inside the site directory.
 */
